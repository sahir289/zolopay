// import { themes } from "./themeUtils";

// export type Themes = (typeof themes)[number];
// // export type Theme = (typeof themes)[number]; // Single theme object type
// // export type ThemeName = Theme["name"]; // Type for just the theme names

